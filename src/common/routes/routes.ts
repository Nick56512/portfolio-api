export const enum RoutingControllerKeys {
  Auth = 'auth',
  User = 'user',
  Portfolio = 'portfolio',
  Image = 'image',
}

export const enum RoutingEndpointKeys {
  Login = 'login',
  Logout = 'logout',
  Registration = 'registration'
}
